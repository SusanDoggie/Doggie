//: Playground - noun: a place where people can play

import Cocoa
import Doggie


if let image = doggie(blendMode: .normal, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .normal, opacity: 1)

if let image = doggie(blendMode: .multiply, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .multiply, opacity: 1)

if let image = doggie(blendMode: .screen, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .screen, opacity: 1)

if let image = doggie(blendMode: .overlay, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .overlay, opacity: 1)

if let image = doggie(blendMode: .darken, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .darken, opacity: 1)

if let image = doggie(blendMode: .lighten, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .lighten, opacity: 1)

if let image = doggie(blendMode: .colorDodge, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .colorDodge, opacity: 1)

if let image = doggie(blendMode: .colorBurn, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .colorBurn, opacity: 1)

if let image = doggie(blendMode: .softLight, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .softLight, opacity: 1)

if let image = doggie(blendMode: .hardLight, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .hardLight, opacity: 1)

if let image = doggie(blendMode: .difference, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .difference, opacity: 1)

if let image = doggie(blendMode: .exclusion, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .exclusion, opacity: 1)

if let image = doggie(blendMode: .plusDarker, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .plusDarker, opacity: 1)

if let image = doggie(blendMode: .plusLighter, compositingMode: .default, opacity: 1).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .plusLighter, opacity: 1)

if let image = doggie(blendMode: .default, compositingMode: .clear, opacity: 0.5).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .clear, opacity: 0.5)

if let image = doggie(blendMode: .default, compositingMode: .copy, opacity: 0.5).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .copy, opacity: 0.5)

if let image = doggie(blendMode: .default, compositingMode: .sourceOver, opacity: 0.5).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .normal, opacity: 0.5)

if let image = doggie(blendMode: .default, compositingMode: .sourceIn, opacity: 0.5).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .sourceIn, opacity: 0.5)

if let image = doggie(blendMode: .default, compositingMode: .sourceOut, opacity: 0.5).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .sourceOut, opacity: 0.5)

if let image = doggie(blendMode: .default, compositingMode: .sourceAtop, opacity: 0.5).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .sourceAtop, opacity: 0.5)

if let image = doggie(blendMode: .default, compositingMode: .destinationOver, opacity: 0.5).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .destinationOver, opacity: 0.5)

if let image = doggie(blendMode: .default, compositingMode: .destinationIn, opacity: 0.5).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .destinationIn, opacity: 0.5)

if let image = doggie(blendMode: .default, compositingMode: .destinationOut, opacity: 0.5).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .destinationOut, opacity: 0.5)

if let image = doggie(blendMode: .default, compositingMode: .destinationAtop, opacity: 0.5).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .destinationAtop, opacity: 0.5)

if let image = doggie(blendMode: .default, compositingMode: .xor, opacity: 0.5).cgImage {
    NSImage(cgImage: image)
}
coregraphic(blendMode: .xor, opacity: 0.5)
